{
    "slug": "field",
    "default": {
        "label": "",
        "type": "text",
        "required": false,
        "autocomplete": true,
        "value": "",
        "icon": false,
        "size": "md",
        "radius": "md"
    },
    "description": {
        "label": "Array of label items like labelText.",
        "type": "text, checkbox and radio",
        "required": "HTML5 validation on fields",
        "autocomplete": "Browser automatically completes the input values based on values that the user has entered before",
        "value": "Field value",
        "icon": "Adds a icon before the input field.",
        "radius": "Rounded edges say you? (none, xs, sm, md, lg)"
    },
    "view": "field.blade.php",
    "dependency": {
        "sass": {
            "components": [
                "fields",
                "icon"
            ]
        }
    }
}
